<UserControl x:Class="VisionPro.App.Views.Config.InputImageConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Configuration Section -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">
                <TextBlock Text="Image Source" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                
                <TextBlock Text="Source Type" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,2"/>
                <ComboBox ItemsSource="{Binding SourceTypes}" SelectedItem="{Binding SelectedSourceType}" Margin="0,0,0,10"/>

                <!-- Conditional Visibility for Source Options -->
                <StackPanel Visibility="{Binding SelectedSourceType, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter='USB Camera'}">
                    <TextBlock Text="Camera Index" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,2"/>
                    <TextBox Text="{Binding CameraIndex}" Margin="0,0,0,10"/>
                    <CheckBox Content="Auto Exposure" IsChecked="{Binding AutoExposure}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,5,0,10"/>
                </StackPanel>

                <StackPanel Visibility="{Binding SelectedSourceType, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter='IP Camera'}">
                    <TextBlock Text="IP Address (RTSP)" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,2"/>
                    <TextBox Text="{Binding IpAddress}" Margin="0,0,0,10"/>
                </StackPanel>

                <StackPanel Visibility="{Binding SelectedSourceType, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter='File'}">
                    <TextBlock Text="File Path" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,2"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding SourcePath}"/>
                        <Button Grid.Column="1" Content="..." Margin="5,0,0,0" Width="30"/>
                    </Grid>
                </StackPanel>

                <TextBlock Text="Image Adjustments" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,20,0,10"/>
                
                <TextBlock Text="Brightness" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Slider Minimum="-100" Maximum="100" Value="{Binding Brightness}" Margin="0,0,0,10"/>

                <TextBlock Text="Contrast" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Slider Minimum="0" Maximum="3" Value="{Binding Contrast}" Margin="0,0,0,10"/>

                <TextBlock Text="Saturation" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Slider Minimum="0" Maximum="3" Value="{Binding Saturation}" Margin="0,0,0,10"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Preview Section -->
        <Border Grid.Column="1" Background="#111" Margin="10" CornerRadius="8">
            <Grid>
                <Image Source="{Binding PreviewImage, Converter={StaticResource ByteArrayToImageConverter}}" Stretch="Uniform"/>
                <TextBlock Text="Live Preview" Foreground="#555" HorizontalAlignment="Center" VerticalAlignment="Center" 
                           Visibility="{Binding PreviewImage, Converter={StaticResource NullToVisibilityConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
