<UserControl x:Class="VisionPro.App.Views.Config.ConditionConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Formula Editor -->
        <GroupBox Grid.Row="0" Header="Formula (C# Logic)" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15">
            <TextBox Text="{Binding Formula}" AcceptsReturn="True" Height="80" VerticalScrollBarVisibility="Auto" 
                     FontFamily="Consolas" FontSize="14" Padding="8" Background="#111" Foreground="Lime"/>
        </GroupBox>

        <!-- Inputs List -->
        <GroupBox Grid.Row="1" Header="Dynamic Inputs" Foreground="{StaticResource TextPrimaryBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding Inputs}" AutoGenerateColumns="False" CanUserAddRows="False"
                          Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimaryBrush}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Variable Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridComboBoxColumn Header="Type" ItemsSource="{Binding AvailableTypes}" SelectedItemBinding="{Binding Type}" Width="100"/>
                        <DataGridTemplateColumn Header="Action" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🗑" Command="{Binding DataContext.RemoveInputCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Background="Transparent" Foreground="{StaticResource NGBrush}" BorderThickness="0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Row="1" Content="➕ Add New Input" Command="{Binding AddInputCommand}" 
                        Margin="5" Padding="8" HorizontalAlignment="Left"/>
            </Grid>
        </GroupBox>

        <!-- Help Info -->
        <TextBlock Grid.Row="2" Text="Note: Variables defined here will be available as input ports on the flow node." 
                   Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
